/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['utfs.io']
  }
  // Uncomment if needed:
  // trailingSlash: true, // Ensures URLs end with a slash
  // distDir: 'dist',     // Change output directory from `out` to `dist`
};

module.exports = nextConfig;
